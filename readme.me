user -> food reels, view food items
Fodd Partner -> list food items

Folder structer:

project/
│── app.js          # Express app setup (engine)
│── server.js       # Start server (ignition key)
│── .env            # Secrets (fuel type info)
│── /db             # DB connection (fuel system)
│── /models         # DB models (blueprints of car parts)
│── /routes         # API routes (roads to drive on)
│── /controllers    # Business logic (driver controlling the car)
│── /middlewares    # Middle layers (traffic police / toll gate)
│── /services       # Extra features (AC, GPS, music system)
│── /utils          # Helpers (tools in glove box)
│── /config         # Settings (dashboard switches)
│── /tests          # Testing (mechanic garage)
│
│── /client         # Frontend (passenger seat / dashboard display)
│   │── /src
│   │   │── /components   # Reusable parts (buttons, seats)
│   │   │── /pages        # Views/pages (windshield view)
│   │   │── /hooks        # Custom logic (car sensors)
│   │   │── /context      # Global state (car brain / ECU)
│   │   │── /services     # API calls (walkie-talkie with driver)
│   │   │── /utils        # Helpers (passenger toolkit)
│   │   │── App.js        # Main UI setup (dashboard wiring)
│   │   │── index.js      # Entry point (start the dashboard)
│   │── /public           # Static assets (stickers, number plate)
